# Feat: Launch Codex workers with composed agent prompts (H-CDX-3)

**Priority:** High
**Source:** Spec `.opencode/plans/1775082497672-eager-rocket.md`
**Depends on:** H-CDX-1
**Domain:** ai-tooling
**Lineage:** 78c9bff0-cf71-46fb-a837-3dd2f16f5448

Update worker launch so Codex sessions receive the selected ninthwave agent instructions even though Codex does not expose the same top-level `--agent` launch flag as the other tools. Compose the canonical agent prompt and the existing `.ninthwave/.prompt` work-item payload before invoking `codex` or `codex exec`, and keep the rest of the launch flow generic.

**Test plan:**
- Extend `test/launch.test.ts` to verify `--tool codex` dispatches the expected interactive and headless command strings through the mux
- Add assertions that the prompt written for Codex includes both the selected canonical agent instructions and the work-item metadata/body already generated by `launch.ts`
- Cover at least one non-default agent path, such as reviewer or rebaser, so Codex prompt composition does not only work for the implementer case

Acceptance: launching a work item with `--tool codex` produces the expected Codex command, preserves the existing work-item prompt content, and prepends the correct ninthwave agent instructions without relying on `AGENTS.md` or a Codex `--agent` flag.

Key files: `core/commands/launch.ts`, `test/launch.test.ts`
