item: H-TA-2
date: 2026-03-29T15:43:33Z
severity: low
description: Env detection ordering was subtle. Naively looping AI_TOOL_PROFILES for env checks broke the "OPENCODE takes priority over CLAUDE_CODE_SESSION" test because Claude's profiles appear first. Required a two-pass approach (exact-value checks before presence checks) to preserve original semantics. The profile ordering doesn't encode this priority, so it wasn't obvious from the data structure alone.
